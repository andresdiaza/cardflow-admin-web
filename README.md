Requisitos del sistema (Node.js versión específica, Angular CLI).

Instrucciones para instalar dependencias (npm install).

Explicación de cómo configurar las variables de entorno de Firebase para el entorno de desarrollo.

Estructura de componentes (módulo de carga masiva, dashboard, login).

Comando para levantar el servidor de desarrollo (ng serve).
